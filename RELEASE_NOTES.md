# Kavita-GDS 0.9.1.4-4

OPDS 만화 스트리밍에서 다음 권을 미리 준비하고, 관리자 사용자 수정 화면의 저장 제한을 수정했습니다. 공식 Kavita `0.9.1.4` 기반이며 기존 사용자 프로필과 DB 구조를 유지합니다.

- OPDS에서 실제 페이지를 읽으면 웹 리더와 같은 순서로 접근 가능한 다음 항목 하나를 준비합니다. 카탈로그 조회만으로 다운로드하지 않으며 진행률이나 프로필을 변경하지 않습니다.
- 중복 요청과 동시 추출을 제한하고 디스크 여유 공간과 실행 그룹의 I/O 압력을 확인합니다. 첫 권을 가져오느라 생긴 일시적 부하는 최대 2분 동안 기다린 뒤 재확인합니다. 기기가 너무 빨리 다음 권으로 넘어가거나 부하가 계속되면 대기가 남을 수 있습니다.
- 관리자 화면에서 기존 이메일이 비어 있거나 과거 형식이어도 이메일을 바꾸지 않는 권한·라이브러리 수정은 저장할 수 있습니다. 새로 입력한 잘못된 이메일은 거부하며, 저장 오류 뒤 재시도가 가능합니다.
- 읽기 전용 계정의 연령 설정 편집 표시를 서버 권한과 맞췄습니다. 일반 사용자 설정과 프로필은 각 계정의 권한을 따릅니다.

기존 OPDS URL, 다운로드 형식, 프로필 DTO 및 DB 스키마는 유지합니다. 폰트 및 프로필 보존 수정은 이전 릴리스의 동작을 포함합니다.

알려진 기존 제한: Android16/MoonReader TXT 회전 시 화면 잘림·읽기 위치 변경은 외부 앱의 동작으로 남아 있습니다. 이번 릴리스가 이를 해결하거나 전체 외부 리더 조합을 검증했다고 주장하지 않습니다.

업데이트 전 config/DB를 백업하세요. 문제가 발생하면 이전 `0.9.1.4-3` 이미지로 되돌릴 수 있으며 DB 복원은 필요한 경우에만 수행하세요.

검증: 서비스 테스트 2,626개(기존 6개 건너뜀), 서버 테스트 102개, 실제 관리자·일반 사용자 화면과 API/DB 검증을 통과했습니다. 캐시 없는 작품의 태블릿 OPDSy 권 전환과 기존 운영 DB 데이터 보존을 확인했습니다. amd64·arm64·armv7에서 새 DB 생성, SQLite·EPUB/TXT/PDF/OPDS 읽기, 프로필 보존과 기존 DB 재기동을 확인했습니다. ARM은 QEMU 10.2.1 검증입니다. 운영 반영 후 SQLite, 기존 프로필·설정 보존, 웹 리더 및 내부·HTTPS OPDS도 확인했습니다.

```bash
docker pull ghcr.io/suikano1304/kavita-gds:0.9.1.4-4
```

- Multi-arch: `sha256:de53923745f2f537640a11086fd61a4ea44cfaba320904849897b699e0db2c83`
- `linux/amd64`: `sha256:c262985af59e3475b6f62760636cb998007df217e024178b3a3bed9119090dae`
- `linux/arm64`: `sha256:b4163c8d7836751306c813a6ddf6c33b4cb9ddf7fcf881f8e886c43f9e75d1a0`
- `linux/arm/v7`: `sha256:2f923058fec0b0f63d8c184ac72b65c570e68d0a1c8a31ccfee3f8ac41295950`

빌드 소스: [eb0078229](https://github.com/suikano1304/Kavita-GDS/commit/eb0078229). UI를 한 번 빌드해 모든 RID에 공유하고 하나의 buildx 흐름으로 이미지를 만들었습니다.
