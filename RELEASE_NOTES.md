# Kavita-GDS 0.9.1.4-5

OPDS 클라이언트가 완독 권의 주변 이미지를 자동으로 가져올 때 서버 진행률이 뒤로 내려가던 문제를 수정했습니다. 공식 Kavita `0.9.1.4` 기반입니다.

- OPDS 이미지 요청은 저장된 진행률보다 앞으로 갈 때만 갱신합니다. 동시·역순 요청도 완독 기록을 낮추지 않으며, 이미지 요청으로 독서 세션을 생성하지 않습니다. Panels와 `saveProgress=false`의 저장 제외는 유지합니다.
- 웹과 OPDS의 이어읽기는 가장 뒤까지 진행한 권을 기준으로 합니다. 90% 이상이면 다음 권을 추천하며, 과거 권의 미완독 기록이나 최근 상태 편집 때문에 추천이 뒤로 돌아가지 않습니다.
- 90%는 추천 기준입니다. 실제 저장 페이지·완독 상태는 일괄 변경하지 않습니다. 웹의 직접 페이지 이동·미독 지정·재독은 유지합니다. OPDS 이미지 선행 요청만으로 실제 표시 페이지를 알 수 없어 서버 위치가 화면보다 앞설 수 있습니다.

호환성: 기존 OPDS URL·파일 형식·프로필 DTO·DB 스키마를 유지합니다. 이어읽기 대상이 없을 때 `GET /api/reader/continue-point`는 **204 No Content**를 반환합니다. 포함된 웹 UI는 이를 처리하며 외부 API 사용자는 빈 응답을 처리해야 합니다. Tachiyomi의 정확한 완독 기준은 90% 추천 기준과 분리합니다.

검증: 전체 서비스 회귀 2,636개와 추가 경계 테스트 2개, 서버 테스트 102개 통과(기존 서비스 제외 6개). 실제 Angular 페이지 이동·저장·재열기, 사용자별 프로필·폰트, 내부/HTTPS OPDS 다운로드·Range·동시 합본을 확인했습니다. OPDSy 실기기에서 완독 권의 역순 이미지 요청 뒤에도 기록이 보존되고 다음 권 추천이 유지됐습니다. amd64·arm64·armv7의 새 DB 초기화, SQLite, EPUB/TXT/PDF/OPDS, 프로필 보존과 재기동을 검증했습니다. ARM은 QEMU 10.2.1 검증입니다.

기존 외부 앱 제한: MoonReader의 TXT 회전 문제와 확대된 외부 리더 조합은 이번 수정으로 해결되거나 전체 재검증된 것으로 표시하지 않습니다.

업데이트 전 config/DB를 백업하세요. 문제 발생 시 이전 `0.9.1.4-4` 이미지로 되돌릴 수 있으며 DB 복원은 필요성이 확인된 경우에만 수행하세요.

```bash
docker pull ghcr.io/suikano1304/kavita-gds:0.9.1.4-5
```

- Multi-arch: `sha256:f03543fe3e6fa7e563ac8ab4e55bd27ffcdcf93d49a342ab1847ef7004dcd500`
- `linux/amd64`: `sha256:8df52962e221a726fb810b663eea75287fa868416b105b100c5810380104304d`
- `linux/arm64`: `sha256:f15b826b7a2e8587d43a4fdfa612035db2635d8a5207283abe9489d5390c0f50`
- `linux/arm/v7`: `sha256:be3b8318da133e96321442bbda204d43f059a641116d756536c5d9236c4c1bac`

빌드 소스: [ead1e2b5e](https://github.com/suikano1304/Kavita-GDS/commit/ead1e2b5e). PVE에서 UI를 한 번 빌드해 세 RID에 공유하고 단일 buildx 흐름으로 조립한 OCI를 검증 후 게시합니다.
